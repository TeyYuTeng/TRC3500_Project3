# TRC3500_Project3

ADC Channel 1: Pin A0 \
ADC Channel 2: Pin A2 

Use `plot_csv.py` to plot graph:

Modify this line for file you want to plot.
```python
# === Settings ===
csv_file = 'thermistor_test(adc1_only).csv'
```